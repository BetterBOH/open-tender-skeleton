{
  "name": "mock",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "open-tender-skeleton": "file:..",
    "react": "^16.6.3",
    "react-dom": "^16.6.3",
    "react-scripts": "^2.1.3"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build && cd .. && build-storybook -c .storybook -o ./mock/build/storybook && cd -",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "devDependencies": {
    "@storybook/react": "^5.0.6",
    "node-sass": "^4.11.0"
  }
}
