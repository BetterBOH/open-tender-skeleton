import React from 'react';

const LocationsSearch = React.memo(props => (
  <div className="col-12 md:col-5 lg:col-3 bg-color-white">
    LocationsSeartch
  </div>
));

export default LocationsSearch;
